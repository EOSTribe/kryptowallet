import * as React from 'react';
import MaterialIcon from 'react-native-vector-icons/MaterialCommunityIcons';

const KIcon = ({ name, size, color }) => (
  <MaterialIcon name={name} color={color} size={size} />
);

export default KIcon;
